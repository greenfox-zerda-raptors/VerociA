import org.junit.Test;

import java.util.ArrayList;
import java.util.Arrays;

import static org.junit.Assert.*;

/**
 * Created by Verőci Ádám on 2016.11.21..
 */
public class SumTest {
    @Test
    public void sumok() throws Exception {
        ArrayList numbers= new ArrayList<>();
        numbers.addAll(Arrays.asList(2, 3, 8, 12));

        ArrayList numbers2= new ArrayList<>();
        numbers2.addAll(Arrays.asList(1));

        ArrayList numbers3= new ArrayList<>();
        numbers3.addAll(Arrays.asList());

      //  assertEquals();
    }
}